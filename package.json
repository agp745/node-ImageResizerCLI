{
  "name": "node-image-resizer-cli",
  "bin": {
    "resize": "./dist/index.js"
  },
  "version": "0.0.1",
  "description": "image resizer cli in node with typescript and ffmpeg",
  "main": "src/index.ts",
  "repository": "git@github.com:agp745/node-ImageResizerCLI.git",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.6",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "dev": "tsc -w",
    "link-cli": "npm unlink --global && npm link",
    "redo": "find test_images -name 'resized-*.jpg' -print0 | xargs -0 rm"
  },
  "dependencies": {
    "commander": "^11.0.0"
  }
}
